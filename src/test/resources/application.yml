server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://di1shuai-2:3306/cmb_question_bank_dev?useSSL=false&characterEncoding=utf8&useUnicode=true&useAffectedRows=true&allowPublicKeyRetrieval=true
    username: root
    password: admin
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
---

# local
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:mysql://di1shuai-2:3306/cmb_question_bank_dev?useSSL=false&characterEncoding=utf8&useUnicode=true&useAffectedRows=true&allowPublicKeyRetrieval=true



---
# dev
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mysql://di1shuai-2:3306/cmb_question_bank_dev?useSSL=false&characterEncoding=utf8&useUnicode=true&useAffectedRows=true&allowPublicKeyRetrieval=true
